## Reflection

- Discussed the **geometric primitives** used for drawing and explored the **graphical framework in Java**.
- Understood the **Event Listener Model** as the design pattern used for developing graphical user interfaces in Java.
- Learned how **curves are connected with calculus**, enabling the mathematical representation of smooth graphical shapes.
- Explored **Git and GitHub integration**, including the differences between **SSH** and **HTTPS** for accessing remote repositories.
- Practiced generating an **SSH key pair** through the terminal and understood the roles of **public** and **private keys** in secure authentication.
- Discussed the tools used for **SSH operations** and learned how SSH allows cloning repositories and pushing changes without repeated authentication.
- Reviewed the algorithm for constructing a **perpendicular to a given line** by finding a point on a parallel line and connecting it with a point on the original line.